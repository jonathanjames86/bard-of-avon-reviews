import React from 'react';
import './Footer.css';

const Header = () => (
  <header className="App-footer">
    <h1 className="App-title">
        Footer Component
    </h1>
  </header>
);

export default Header;